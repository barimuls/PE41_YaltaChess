# Créer le plateau
from Plateau import creer_plateau
from IA_orienté_objet import *

plateau = creer_plateau()
plateau.remplir_arete()
plateau.remplir_pieces_initiales()

# Créer les joueurs
joueur0 = JoueurHumain(0)
joueur1 = IAMinimax(1, profondeur=1, evaluateur=EvaluateurComplet(), optimise=True)
joueur2 = IAMinimax(2, profondeur=2, evaluateur=EvaluateurComplet(), optimise=True)

# Créer le gestionnaire
jeu = GestionnaireJeu(plateau, [joueur0, joueur1, joueur2])

# Lancer la partie
jeu.jouer_partie_complete(mode='console')