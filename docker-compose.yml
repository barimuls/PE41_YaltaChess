version: '3.8'

services:
  my-python-app:
    build: .
    container_name: pe41
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      # Maps the current folder on your server to the /app folder in the container
      - .:/app
